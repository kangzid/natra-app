{
  "name": "natra-mobile",
  "version": "1.0.0",
  "description": "NATRA Employee Mobile App — Nata Terpadu Rapi Amanah",
  "main": "index.html",
  "scripts": {
    "dev": "npm-run-all --parallel dev:css dev:server",
    "dev:css": "tailwindcss -i ./src/styles/input.css -o ./src/styles/tailwind.css --watch",
    "dev:server": "http-server . -p 3000 -c-1 --cors",
    "build:css": "tailwindcss -i ./src/styles/input.css -o ./src/styles/tailwind.css --minify",
    "prep:www": "powershell -Command \"if (Test-Path www) { Remove-Item -Path www -Recurse -Force }; New-Item -ItemType Directory -Path www; Copy-Item index.html www/; Copy-Item pages www/ -Recurse; Copy-Item src www/ -Recurse\"",
    "cap:add:android": "pnpm run build:css && pnpm run prep:www && pnpm cap add android",
    "cap:sync": "pnpm run build:css && pnpm run prep:www && pnpm cap sync",
    "cap:run:android": "pnpm cap run android",
    "cap:open:android": "pnpm cap open android",
    "build:apk": "cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0",
    "@capacitor/local-notifications": "^6.1.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.19"
  },
  "keywords": [
    "natra",
    "employee",
    "hris",
    "capacitor",
    "mobile"
  ],
  "author": "NATRA Team",
  "license": "MIT"
}
